
//array to store the value which is calculated.......
int value[100];
memset(value,0,sizeof(value));//initialize the array with 0

int maxChainLen(struct val p[],int n)
{
    //check if for n it is already calculated or not<<<<<<<<<<<
    if(value[n]!=0) return value[n];
    
    
  
}
